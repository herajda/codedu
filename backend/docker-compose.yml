version: '3.8'
services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: codeedu_user
      POSTGRES_PASSWORD: supersecret
      POSTGRES_DB: codeedu_db
    ports:
      - "48812:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
